tc = int(input())

for _ in range(tc):
    OX = input()
    ans = 0
    list_OX = OX.split('X')

    for i in range(len(list_OX)):
        for j in range(1, len(list_OX[i])+1):
            ans += j

    print(ans)


