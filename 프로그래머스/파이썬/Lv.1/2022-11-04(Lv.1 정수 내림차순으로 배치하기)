def solution(n):
    '''
    answer = []
    for i in str(n):
        answer.append(i)
    answer.sort(reverse=True)
    
    result = ""
    for j in answer:
        result += j
    '''
    ans = list(str(n))
    ans.sort(reverse=True)
    return int("".join(ans))
