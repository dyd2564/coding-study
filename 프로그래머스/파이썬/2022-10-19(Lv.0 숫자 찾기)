def solution(num, k):
    answer = 0
    num = list(map(int, str(num)))
    for i in range(len(num)):
        if k == num[i]:
            return i+1
        else:
            answer = -1
    return answer
